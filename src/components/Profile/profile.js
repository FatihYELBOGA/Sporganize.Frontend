import React from 'react';

const Profile = () => {
  return (
    <div style={{
      display: 'flex',
      justifyContent: 'center',
      alignItems: 'center',
      height: '100vh'
    }}>
      Bu bir profil sayfasıdır.
    </div>
  );
}

export default Profile;
